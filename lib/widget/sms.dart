import 'package:flutter/material.dart';

class SMS extends StatelessWidget {
  const SMS({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hello from sms"),
    );
  }
}